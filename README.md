# Hexed plugin for Mindurka

[Workspace](https://github.com/Darkdustry-Coders/Workspace) is required to build and run this project.

## Running

```bash
./b run hexed
```

Or if RabbitMQ is running separately:

```bash
./b run hexed --rabbitmq "amqp://guest:guest@localhost:YOURPORT/%2F"
```